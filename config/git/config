[diff]
    tool = nvimdiff
    colorMoved = zebra
[difftool "nvimdiff"]
    cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""
[difftool]
    prompt = false
[user]
    name = Kieran O'Hara
[core]
    filemode = false
    pager = less -F -X
[merge]
[mergetool]
    prompt = false
[color]
    ui = auto
[push]
    default = upstream
    autoSetupRemote = true
[rebase]
    instructionFormat = "%s (%an)"
    autosquash = true
    autoStash = true
[pull]
    rebase = true
[fetch]
    prune = true

[alias]

    ab = "!git absorb"

    am = "!git-branchless amend"
    hide = "!git-branchless hide"
    ll = "!git-branchless smartlog"
    mov = "!git-branchless move"
    nn = "!git-branchless next"
    np = "!git-branchless prev"
    rc = "!git-branchless record"
    rci = "!git-branchless record -i"
    rw = "!git-branchless reword"

    pr = "!gh pr create"
    prm = "!gh pr list --author='@me'"
    pro = "!gh pr view --web"
    br = "!gh browse"

    a = add
    b = branch
    c = commit
    co = checkout
    cp = cherry-pick
    d = diff
    dc = diff --cached
    detach = checkout --detach
    dt = difftool
    dtc = difftool --cached
    f = fetch
    l = pull
    p = push
    pf = push --force-with-lease origin HEAD
    rb = rebase
    rba = rebase --abort
    rbc = rebase --continue
    rbi = rebase --interactive
    rbim = rebase master --interactive
    rh = reset --hard
    rs = restore
    s = status
    sl = "!git branch --merged | grep  -v '\\*\\|master\\|main' | xargs -n 1 -r git branch -d"
    sm = submodule
    smur =  submodule update --recursive
    smuri =  submodule update --recursive --init
    smurr =  submodule update --recursive --remote
    sta = stash
    stp = stash pop
    unstage = restore --staged
    x = "!gitx"
[init]
	defaultBranch = main
[status]
	submodulesummary = 1

[includeIf "gitdir:/Volumes/Source/dotfiles/"]
	path = personal
[includeIf "gitdir:/Volumes/Source/personal/**"]
	path = personal
[credential]
	helper =
	helper = /usr/local/share/gcm-core/git-credential-manager
[credential "https://dev.azure.com"]
	useHttpPath = true
